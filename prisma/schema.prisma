generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Property {
  id            Int    @id @default(autoincrement())
  title         String
  city          String
  details       String
  pricePerNight Int
  description   String
}